[pytest]
addopts = -p no:flask
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    asyncio: mark test as async
    live_upstream: requires a running CEOS upstream service (construction / erp)
